// webpack.config.js
module.exports = {
    // ... other configurations
    resolve: {
      alias: {
        'swiper': 'swiper/bundle', // or the correct path to the swiper bundle
      },
    },
  };